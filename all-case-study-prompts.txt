ALL CASE STUDY PROMPTS — Wireshark Network Analysis
============================================================

────────────────────────────────────────────────────────────
Chapter 01: The World of Network Analysis
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] Network analysis requires three skills: understanding TCP/IP, comfort with Wireshark,
       and familiarity with packet structures and typical packet flows. Which of these three
       is the hardest to develop through reading alone — and what does a "baseline of
       understanding" actually mean in practice?

  [2] The analysis example shows a client browsing to www.wireshark.org. When the download
       becomes painfully slow, the book says "you'd have known the answer long before" if
       Wireshark was running. But Wireshark captures thousands of packets per second — how
       would you actually pinpoint the slowdown among all that traffic?

  [3] The troubleshooting walk-through moves Wireshark to a second location (just off the
       server) and finds that Mike's TCP handshake does NOT show SACK support — even though
       Mike's machine actually supports it. The server had been behaving correctly all
       along. Why would moving the capture location reveal something completely different
       from the first capture?

  [4] In Colton's investigation, the malicious application was communicating on TCP port
       18067 instead of the standard IRC port 6667. The commands (USeR, NiCK, JOiN) used
       mixed case instead of all-caps. What does altering both the port number AND the
       command casing tell you about the attacker's threat model — and what did Wireshark
       reveal that IDS/IPS might have missed?

  [5] The book divides analysis tasks into four types: Troubleshooting, Security,
       Optimization, and Application Analysis — and distinguishes between proactive and
       reactive analysis. Reactive analysis is described as more common. Why would proactive
       analysis (finding problems before users notice) be less common, and what would make
       it more feasible in practice?

  [6] Figure 5 shows routers changing the destination MAC address while keeping the IP
       address the same. Switches (Figure 4) forward packets without changing either. Why
       does a router change the MAC address but preserve the IP — and what would go wrong in
       a network if a router preserved the MAC address instead?

  [7] The same capability that lets a network analyst diagnose problems — capturing
       unencrypted traffic — can also expose passwords and confidential data to malicious
       eavesdroppers. Is this fundamentally a tool problem or a network design problem, and
       what does the answer imply about how security should be approached?

  [8] Seven techniques are listed to avoid the "needle in a haystack" problem when faced
       with thousands of packets. The book orders them: placement → capture filters →
       display filters → colorize → reassemble streams → save subsets → graphs. Why does
       placement come first — and what does that order tell you about where the real power
       lies?

  [9] Step 5 of the launch process says: "If your system supports both IPv4 and IPv6, you
       may see two DNS queries." What does seeing ONLY one DNS query (for the A record, not
       AAAA) tell you about the client machine — and if you see NO DNS queries at all before
       a TCP connection, what does that imply?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. A network engineer says "the router logs show no errors." A user says "the web page
       loads slowly." Which source do you trust more, and why?

  Q2. Name two types of problems that packet analysis can detect that SNMP and logs cannot.

  Q3. A security analyst and a network analyst are both looking at the same capture. What
       would each one focus on first?

  Q4. If an application analyst sees high response times but no TCP retransmissions, where
       should they look next?

  Q5. You're investigating a suspected data exfiltration. Should you use a TAP or a SPAN
       port, and why?

  Q6. A SPAN port shows a connection was established, but the user says the application
       never connected. How could a SPAN port cause this discrepancy?

  Q7. A packet leaves a client and passes through two routers before reaching a server. What
       changes in the Ethernet header at each router, and what stays the same?

  Q8. Wireshark captures at Layer 2. What information is available to you that would NOT be
       visible if you captured only at Layer 3 (IP)?


────────────────────────────────────────────────────────────
Chapter 02: Introduction to Wireshark
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] Gerald Combs created Wireshark because commercial analyzers cost $5,000–$20,000 and
       his employer limited him to tcpdump and snoop. tcpdump already existed as an
       open-source tool when he started. Why build an entirely new GUI application instead
       of improving tcpdump — and what does that design choice reveal about where the real
       gap in network analysis tooling was?

  [2] WinPcap, libpcap, and AirPcap are all described as "link-layer interfaces" but they
       are not interchangeable. What determines which one is used — and crucially, why does
       Wireshark need a completely separate library (the Wiretap Library) just to OPEN saved
       trace files when these capture drivers already handle raw packets?

  [3] The Wiretap Library lets Wireshark open dozens of trace file formats from competing
       products. From a practical standpoint, why would an organization ever need to open a
       trace file from a competing product rather than just recapturing the traffic
       themselves?

  [4] Wireshark dissectors chain together — each one reads a type field and hands off to the
       next dissector. If you disable the UDP dissector in Analyze | Enabled Protocols, what
       happens to DNS and DHCP traffic, and why does this reveal something fundamental about
       how protocol layering actually works in Wireshark?

  [5] The Status Bar in Wireshark shows five distinct elements. If you were designing a
       training exercise for a new analyst opening a trace file for the first time, which
       single Status Bar element would you tell them to check first — and what would it
       immediately reveal about the capture without selecting any packet?

  [6] The Analyze menu contains "Decode As" which forces a specific dissector onto
       non-standard port traffic. The Statistics menu contains IO Graphs which can overlay
       display-filtered traffic against total traffic. If you could only teach a new analyst
       two menu items to accelerate their analysis, which two would you choose — and what
       does each reveal that nothing else can?

  [7] The Main Toolbar has seven sections. The Navigation section includes "Go to
       Corresponding Packet" which links ACKs to their data packets. Why is this
       particularly valuable for TCP analysis — and what would you have to do manually to
       find the corresponding packet without it?

  [8] Right-clicking in the Packet Details pane offers both "Apply as Filter" and "Prepare a
       Filter" — and also "Apply as Column." These three options are easy to confuse.
       Describe a specific analysis scenario where you would use each of the three options,
       and explain why the other two would be inferior choices in each case.

  [9] Wireshark maintains five mailing lists, a Q&A site (ask.wireshark.org), and multiple
       trace file repositories. A new analyst gets stuck on a protocol behavior they cannot
       explain. What is the most efficient path from confusion to an answer — and why does
       the TYPE of question determine where you look?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. Wireshark shows 10,000 packets in the Packet List pane. What is the fastest way to get
       to a specific packet you know is around packet number 7843?

  Q2. What is the difference between clicking "Stop" in a live capture and clicking
       "Restart"?

  Q3. The Protocol column shows "TCP" for a packet, but you can see port 80 in the Info
       column. Why might Wireshark not show "HTTP" for this packet?

  Q4. The Info column shows "[TCP Retransmission]" for a packet whose Protocol column says
       "HTTP." What does this tell you about that packet?

  Q5. You click on a field in the Packet Details pane labeled "Time to live: 64". What
       happens in the Bytes pane, and what does it tell you?

  Q6. What's the difference between right-clicking a field in Packet Details and choosing
       "Apply as Filter" vs "Prepare a Filter"?

  Q7. Where in the Wireshark menus would you find Conversations and Endpoints views?

  Q8. The status bar shows "Packets: 5,243 · Displayed: 12." What does this mean, and how
       did you get there?


────────────────────────────────────────────────────────────
Chapter 03: Capture Traffic like a Pro
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] If you pick the wrong network interface, you'll capture nothing useful. How do you
       identify which interface carries the traffic you need, and why might promiscuous mode
       alone not be enough on a switched network?

  [2] The Capture Options dialog (Ctrl+K) has many settings. Which three settings are most
       critical to configure for a long-running unattended capture, and why?

  [3] During a high-rate live capture you notice 'Dropped: 12' in the status bar. What does
       this mean and what are your options to fix it without stopping the capture?

  [4] You're on a switched network. List four ways to capture other hosts' traffic and give
       one limitation of each.

SOCRATIC QUESTIONS (from Study Guide):

  Q1. You're troubleshooting a web application running on your own laptop (server and client
       both local). Which interface should you capture on, and why?

  Q2. On the Wireshark Welcome Screen, you see several interfaces with flat sparklines (no
       activity) and one with a rapidly fluctuating sparkline. What does this tell you?

  Q3. You enable promiscuous mode on your NIC. You're connected to a managed switch with no
       SPAN port configured. Will you see other hosts' traffic? Why or why not?

  Q4. What is the snapshot length (snaplen), and why would you ever want to set it to
       something smaller than the default?

  Q5. The status bar during a live capture shows a non-zero "Dropped" count. What does this
       mean, and what are two ways to reduce it?

  Q6. You need to run a continuous capture for 48 hours but can only store 10 GB of capture
       data. How would you configure ring buffers to accomplish this?

  Q7. You start a capture after the browser is already open and the page is already
       partially loaded. What specific information did you likely miss?

  Q8. You're analyzing an asymmetric routing problem. Why would a single-sided capture be
       insufficient, and what would capturing on both client and server tell you that one
       side alone cannot?


────────────────────────────────────────────────────────────
Chapter 04: Capture Filters BPF Syntax
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] Capture filters run in the OS kernel before Wireshark sees a packet. Why does this
       fundamentally differ from display filters, and what is the major tradeoff?

  [2] BPF primitives have three qualifier types. Given the expression 'dst port 443',
       identify each qualifier and what happens if you omit the direction qualifier.

  [3] BPF operator precedence: not > and > or. What does 'host 10.1.1.1 and port 80 or port
       443' actually match? How do you fix it?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. You apply a capture filter for only TCP port 80. Later, you discover the failure you
       were troubleshooting was caused by a DNS resolution issue. What did you lose, and
       what does this tell you about aggressive capture filtering?

  Q2. Why do capture filters use BPF syntax while display filters use Wireshark's own filter
       language? What does each language need to be optimized for?

  Q3. Write a BPF capture filter to capture only traffic between hosts 10.1.1.1 and 10.1.1.2
       on TCP port 443. Think through the logic before checking.

  Q4. What is the difference between port 80 and tcp port 80 as a BPF capture filter?

  Q5. What does not host 10.0.0.1 or host 10.0.0.2 actually capture, given BPF precedence
       rules? Is it the same as not (host 10.0.0.1 or host 10.0.0.2)?

  Q6. Write a BPF filter that captures all TCP traffic from the 192.168.0.0/16 subnet going
       to port 53 (DNS over TCP). What does this tell you that a simpler filter could not?

  Q7. You're monitoring a VoIP system that uses SIP (port 5060) and RTP (ports 16384–32767).
       Write a single BPF filter that captures both protocols.

  Q8. Why would you use not broadcast and not multicast as a baseline capture filter on a
       busy LAN, and what types of traffic does it remove?


────────────────────────────────────────────────────────────
Chapter 05: Configure Preferences
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] Wireshark preferences are stored per profile. Why does this matter for analysts who
       work with different protocol environments?

  [2] For troubleshooting TCP performance, what columns would you add beyond the default,
       and what's the fastest way to add a custom column?

  [3] Name resolution translates IPs to hostnames. What are the hidden costs of enabling
       live DNS resolution during a live capture?

  [4] Two protocol preference settings can make the difference between seeing useful decoded
       data versus raw bytes. What are they?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. A colleague's Wireshark looks completely different from yours — different colors,
       columns, and protocol decoding. You're both using the same version. What's the most
       likely cause?

  Q2. Where are personal Wireshark preferences stored, and why does this matter when you
       move to a new computer?

  Q3. You're troubleshooting application latency. Which single column addition would give
       you the most immediate insight into where delays are occurring?

  Q4. You want to always see the TCP stream index in your packet list without manually
       adding it each time. What's the most efficient way to make this permanent?

  Q5. You're doing a live capture and Wireshark is sending DNS queries. What setting is
       causing this, and how do you stop it?

  Q6. MAC address name resolution shows "Cisco_xx:xx:xx" for a device. What does this tell
       you, and what does it not tell you?

  Q7. Wireshark shows all TCP sequence numbers starting at 0. A colleague says the actual
       ISN was 2,847,123,091. Who's right?

  Q8. You need to decrypt captured HTTPS traffic. What file do you need, and how do you
       configure Wireshark to use it?


────────────────────────────────────────────────────────────
Chapter 06: Colorize Traffic
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] Coloring rules are evaluated top-to-bottom and the first match wins. A TCP
       retransmission appears red even though a later 'TCP' rule would color it green. Why,
       and what does this mean for rule ordering?

  [2] What is the 'Bad TCP' default coloring rule's filter expression, and why does it come
       first in the list?

  [3] You want to highlight all HTTP 4xx and 5xx error responses in bright orange. Write the
       filter and explain where to place it in the rule list.

SOCRATIC QUESTIONS (from Study Guide):

  Q1. Wireshark has two coloring rules: Rule A filters http and Rule B filters tcp. A single
       HTTP packet matches both. Which color does it get, and why?

  Q2. You want to hide all color coding temporarily without deleting your rules. What's the
       fastest way?

  Q3. You see a cluster of black-on-red packets in your capture. Before looking at the
       packet details, what do you already know just from the color?

  Q4. The default HTTP rule colors packets light green. But an HTTPS (port 443) packet is
       colored light blue (TCP). Why doesn't the HTTP rule match HTTPS?

  Q5. You want to highlight all packets where a DNS response returned an error. What display
       filter would you use as a color rule, and what color would you choose to make it
       stand out?

  Q6. A colleague sent you their Wireshark coloring rules file. How do you import it?

  Q7. You open a capture and see mostly light-blue TCP packets with occasional black-on-red.
       What should be your first analysis step?

  Q8. Why would you want different coloring rule sets for security analysis vs network
       performance analysis?


────────────────────────────────────────────────────────────
Chapter 07: Time Values and Summaries
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] Wireshark offers absolute, relative, and delta time formats. When is each most useful,
       and which is best for finding a 3-second delay in an HTTP transaction?

  [2] Atime reference (Ctrl+T) sets a packet's timestamp to 0.000000. How is this more
       useful than switching to relative time format?

  [3] You see a 4-second delta before an HTTP 200 OK response. How do you determine whether
       the delay is network transit time or server processing time?

  [4] You open Statistics → Capture File Properties. What three pieces of information tell
       you whether a capture is complete and suitable for analysis?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. You're correlating a Wireshark capture with a firewall log that uses wall-clock
       timestamps. Which time display format should you use in Wireshark?

  Q2. What is the difference between "Seconds Since Previous Captured Packet" and "Seconds
       Since Previous Displayed Packet"? When would the two differ?

  Q3. You want to measure exactly how long a complete HTTP transaction takes from first SYN
       to last data packet. How would you use Time References to do this?

  Q4. After setting a time reference, do you need to save the file for the reference to
       persist? Why or why not?

  Q5. You see a packet with a delta time of 3.2 seconds occurring right after a client sends
       an HTTP GET request. What does this suggest about where the problem is?

  Q6. You filter to show only TCP packets for a specific stream. Should you use "delta since
       previous captured" or "delta since previous displayed" to measure timing within that
       stream? Why?

  Q7. You open a capture and the statistics show average packet size of 62 bytes. What kind
       of traffic is likely dominant in this capture?

  Q8. The Capture File Properties shows a "capture filter" was applied. How does this affect
       your analysis conclusions?


────────────────────────────────────────────────────────────
Chapter 08: Trace File Statistics
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] You open Protocol Hierarchy and see 'Data' listed under TCP representing 40% of
       traffic. What does this mean, and what are your next three investigation steps?

  [2] The Conversations window shows one IP pair responsible for 80% of bytes. What are
       three different explanations, and what additional information would you check to
       distinguish them?

  [3] IO Graphs plot packet or byte rate over time. How would you use IO Graphs to identify
       the exact moment a SYN flood attack started in a 10-minute capture?

  [4] The Flow Graph shows packets as arrows on a ladder diagram. For what type of analysis
       is this uniquely suited compared to the packet list?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. You open a capture and run Protocol Hierarchy. ARP accounts for 34% of all packets.
       What are the two most likely causes, and how would you distinguish between them?

  Q2. You run Protocol Hierarchy and see a protocol called "Data" under TCP at 15% — what
       does this usually mean?

  Q3. In the Conversations view (TCP tab), you see one conversation with 50,000 packets but
       only 20KB total — another with 100 packets but 200MB. What does each pattern suggest?

  Q4. How do you filter the entire Wireshark packet list to show only traffic between two
       specific hosts, using the Conversations dialog?

  Q5. You add two lines to IO Graphs — one for all traffic and one for tcp.analysis.flags.
       What useful information does comparing these two lines give you?

  Q6. The IO Graph shows a traffic spike at t=45 seconds, then drops to near zero. What are
       the two most likely causes of this pattern?

  Q7. What specific information does the TCP Time-Sequence graph reveal that the standard
       packet list doesn't show clearly?

  Q8. When would you use the Flow Graph (Statistics → Flow Graph) vs the TCP Stream Graph
       (Statistics → TCP Stream Graphs)?


────────────────────────────────────────────────────────────
Chapter 09: Display Filters Find Anything
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] Display filters hide packets that don't match — but don't delete them. What are two
       key workflows where this 'non-destructive' behavior is essential?

  [2] Display filter syntax uses field names and comparison operators. What is the
       difference between '==' and 'contains', and when would you use 'matches' with a
       regex?

  [3] Wireshark lets you save display filters as buttons in the toolbar. How does having a
       pre-built filter library change your analysis workflow?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. The display filter bar turns yellow while you're typing. What does this mean, and
       should you apply the filter?

  Q2. You apply a display filter and the packet count drops from 10,000 to 5. You then clear
       the filter. How many packets are in the file now?

  Q3. What is the difference between ip.addr == 10.0.0.1 and ip.src == 10.0.0.1 or ip.dst ==
       10.0.0.1? Are they equivalent?

  Q4. You want to find all packets where a specific string "login" appears anywhere in the
       payload. What display filter would you write?

  Q5. You want to see only NEW TCP connection attempts — no data segments, no ACK-only
       packets. What filter would you use?

  Q6. Write a display filter to find all DNS queries (not responses) for the domain name
       "example.com".

  Q7. What is the difference between "Apply as Filter → Selected" and "Prepare as Filter →
       Selected" when right-clicking a packet field?

  Q8. You type tcp. in the filter bar and press Tab. What happens, and why is this useful?


────────────────────────────────────────────────────────────
Chapter 10: Follow Streams
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] Follow TCP Stream reassembles all TCP segments for a connection and shows the
       application-layer data. Why does this matter for protocols like HTTP and FTP that
       span multiple TCP segments?

  [2] UDP has no connection concept. How does Wireshark define a 'UDP stream' for the Follow
       UDP Stream feature?

  [3] Wireshark can decrypt TLS using either an RSA private key or a pre-master secret log
       file. What is the fundamental advantage of the SSLKEYLOGFILE approach over the RSA
       key approach?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. You Follow a TCP Stream and see only garbled binary characters instead of readable
       text. What are two possible causes?

  Q2. You close the Follow TCP Stream window and notice the main packet list is now
       filtered. What happened, and how do you return to seeing all packets?

  Q3. A DNS query for "evil.example.com" appears in your capture. Using Follow UDP Stream,
       what would you look for to determine if this is suspicious?

  Q4. You're analyzing a TFTP file transfer. Why is Follow UDP Stream more useful than
       looking at individual packets?

  Q5. Your colleague set SSLKEYLOGFILE before starting Chrome but forgot to restart
       Wireshark. The TLS traffic shows as encrypted. What went wrong?

  Q6. A server uses TLS 1.3. Can you decrypt the traffic with the server's private RSA key?
       Why or why not?

  Q7. During forensic analysis, you want to recover a file that was downloaded over HTTP.
       What Wireshark feature do you use, and where do you find it?

  Q8. You see in a Follow TCP Stream that an HTTP response starts with "HTTP/1.1 200 OK"
       followed by what looks like HTML — but also includes a password field. What
       conclusion do you draw, and what should you report?


────────────────────────────────────────────────────────────
Chapter 11: Customize Profiles
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] Wireshark profiles store all per-analyst configuration. Why is using profiles better
       than having a single shared configuration for an entire team?

  [2] When creating a new profile, Wireshark asks whether to copy settings from the current
       profile or start from scratch. When would you choose each option?

  [3] You want to share your entire VoIP analysis profile with a new team member. What is
       the simplest mechanism, and what should you verify after they import it?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. You're analyzing VoIP traffic but your current Wireshark is configured for HTTP
       analysis. What is the fastest way to switch to your VoIP profile without restarting
       Wireshark?

  Q2. If you change a coloring rule while using a specific profile, does it affect other
       profiles? Why or why not?

  Q3. You create a new profile while your "Security" profile is active. What settings does
       the new profile start with?

  Q4. Where on the filesystem would you find the configuration files for a profile named
       "Forensics"?

  Q5. A colleague's Wireshark shows custom "decode-as" rules that map unusual ports to
       protocols. If you import their profile, what file inside the profile directory
       contains those mappings?

  Q6. You delete all your display filters from the filter bar and then switch profiles. Does
       the other profile still have its saved display filters? Why?

  Q7. You want to distribute a standard Wireshark configuration to 20 analysts joining your
       team. What is the most efficient method?

  Q8. You share a profile zip with a colleague who uses Windows while you use macOS. Will
       the profile work correctly after import?


────────────────────────────────────────────────────────────
Chapter 12: Annotate and Export
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] pcapng format supports both packet-level and capture-level comments. Why is this
       valuable for forensics and incident response?

  [2] What is the difference between Ctrl+S and File → Export Specified Packets, and when
       would you use the export function rather than save?

  [3] File → Export Objects → HTTP extracts all transferred objects from a capture. Beyond
       finding files, what security-relevant information can you extract this way?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. You found the exact packet that shows a TCP RST caused by a firewall timeout. Before
       sharing the capture file with your team, how do you highlight that specific packet?

  Q2. After adding packet-level comments to 15 packets, you save the file as .pcap. What
       happens to those comments?

  Q3. What is the primary advantage of .pcapng over .pcap format?

  Q4. A colleague needs to open your capture in an older tool that only supports .pcap. How
       do you save a compatible copy while preserving the original .pcapng?

  Q5. You have a 50,000-packet capture but only need to share the 200 packets matching your
       current display filter. What is the fastest way to create a new capture file
       containing only those 200 packets?

  Q6. During a forensic investigation, you believe a file was transferred over HTTP. What
       Wireshark feature extracts that file?

  Q7. You've marked 8 packets that clearly demonstrate a TCP RST storm. How do you create a
       PDF document showing just those 8 packets with their full protocol decodes?

  Q8. If you close Wireshark without saving after marking packets, are the marks preserved?


────────────────────────────────────────────────────────────
Chapter 13: Expert Information System
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] The Expert Information System flags protocol anomalies automatically. Why is it more
       useful to open Expert Information BEFORE reading through the packet list?

  [2] Expert Information uses four severity levels: Error, Warning, Note, and Chat. Give a
       specific example of a protocol event at each level.

  [3] You want to show only packets that Wireshark flagged as Expert events of Warning level
       or higher. How do you build this display filter?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. You open a capture and the expert status bar badge is red. Before reading a single
       packet, what do you already know?

  Q2. There are 2,000 packets in a capture but the Expert Information dialog shows only 12
       events. What does this tell you about the other 1,988 packets?

  Q3. Expert Information shows a Warning for "TCP Retransmission." Is this always a network
       problem? What additional context do you need?

  Q4. What is the most likely reason Expert Information shows many Error-level checksum
       events when you capture your own machine's traffic?

  Q5. You're troubleshooting slow HTTP responses. Which Expert Information event group
       should you check first?

  Q6. You see both Sequence-group Warnings and Application-group Warnings. What does the
       combination of TCP retransmissions AND HTTP 503 responses suggest about the root
       cause?

  Q7. Write a display filter that shows only packets with Expert Warning or higher severity.

  Q8. The expert system shows "TCP Previous segment not captured." Is this always actual
       packet loss, or could it be a Wireshark artifact?


────────────────────────────────────────────────────────────
Chapter 14: TCP/IP Analysis Overview
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] The TCP/IP stack is described as layered. Network faults are often attributed to the
       TCP/IP protocol or application layer. If a client can successfully ARP and receive a
       response, but cannot complete a DNS query, which layer has a problem — and what would
       your next move be in Wireshark?

  [2] The book says: "Before analyzing traffic to identify faults, you need to know what is
       considered normal network communication." This sounds obvious, but what does "normal"
       actually mean in practice — and how would you establish a baseline for a network
       you've never seen before?

  [3] TCP/IP uses up to six resolution steps before a single packet can be sent. Three of
       these steps generate network traffic (visible in Wireshark) and three do not. If you
       open a trace file and see a TCP SYN as the very first packet — no ARP before it, no
       DNS before it — what can you conclude about the state of the client's caches, and how
       confident can you be in that conclusion?

  [4] Figure 183 shows http-riverbed-one.pcapng (visiting www.riverbed.com after clearing
       DNS and browser cache). There are NO ARP queries, DNS queries appear, then a TCP SYN.
       A second trace (http-riverbed-two.pcapng, 89 seconds later) has many fewer DNS
       queries. What does the different DNS behavior between the two traces tell you — and
       what should you infer about the DNS TTL values in the first trace?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. When Wireshark shows you a frame, it expands into multiple protocol headers. Which
       layer of the TCP/IP model owns each of those headers, and what is each header's job?

  Q2. A packet has an IP TTL of 1 and is traveling through a router. What happens — and
       which layer makes that decision?

  Q3. What is the exact sequence of protocol steps a host performs before it can send the
       first TCP SYN packet to a remote web server by hostname?

  Q4. ARP only works on the local subnet. How does a host reach a server on a different
       network — and what role does the default gateway play in the ARP conversation?

  Q5. What should you do first when you open an unfamiliar trace file — before applying any
       display filters or diving into individual packets?

  Q6. Why is it important to establish a baseline of "normal" traffic before trying to
       identify problems?

  Q7. In a trace file, you see a TCP SYN immediately followed by a TCP RST from the
       destination. What does this tell you about the destination host?

  Q8. A host sends repeated DNS queries but you never see any DNS responses in the trace.
       What are the two most likely explanations?


────────────────────────────────────────────────────────────
Chapter 15: Analyze DNS Traffic
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] DNS translates domain names to IP addresses. What is the typical sequence of events
       when a user browses to a website, and what happens if the DNS step fails?

  [2] Anormal DNS query/response pair takes how long? What would you expect to see in a
       healthy network, and what does an unusually slow response look like in Wireshark?

  [3] Auser reports 'websites don't load but I can ping the server by IP'. What does this
       tell you, and what Wireshark filter would you apply to diagnose the DNS issue?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. Why does DNS use UDP by default instead of TCP, and what specific conditions force it
       to switch to TCP?

  Q2. What happens at the client when a DNS response is truncated under RFC 1035's 512-byte
       UDP limit?

  Q3. How does mDNS differ from standard DNS in terms of transport, addressing, and the
       names it resolves?

  Q4. When a client receives a CNAME record in a DNS response, is the lookup complete? What
       must happen next?

  Q5. Why would a host send both an A record query AND an AAAA record query for the same
       hostname at the same time?

  Q6. In recursive querying, who does the "heavy lifting" of finding the answer — the client
       or the DNS server? How does this differ from iterative querying?

  Q7. DNS queries are being sent but you see ICMP Destination Unreachable (Port Unreachable)
       replies on port 53. What does this tell you, and what should you check first?

  Q8. What's the practical difference between an NXDOMAIN and a SERVFAIL from a
       troubleshooting perspective?

  Q9. Why does the DNS client double its retry interval each time it resends an unanswered
       query (1s, 1s, 2s, 4s…)?

  Q10.  What purpose does the Transaction ID serve in DNS, and how does Wireshark use it to
       help analysts?

  Q11.  Why does DNS have both an "Answer RR" section AND an "Authority RR" section? When
       would each contain data?

  Q12.  What's the real-world impact of a very short TTL value in DNS responses — who is
       affected and how?

  Q13.  Why can't you use "dns" as a capture filter — and what must you use instead? Why
       does this limitation exist?

  Q14.  The filter dns.flags.rcode != 0 shows a yellow background in Wireshark when you type
       it. Is this a problem? Why does Wireshark warn about != ?

  Q15.  How would you build a filter to show only DNS responses that contain more than one
       answer record — suggesting a CNAME chain?

  Q16.  How did the analyst prove the problem was DNS — not the web server or the network
       path — causing the slow page loads?

  Q17.  Why did some pages load fast (with no delay) even while the problem was active?

  Q18.  What DHCP configuration change would have permanently fixed the problem in this case
       study?


────────────────────────────────────────────────────────────
Chapter 16: Analyze ARP Traffic
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] ARP resolves an IP address to a MAC address on the local network. Why is this
       necessary, and what happens when a host wants to communicate with a machine on a
       different subnet?

  [2] Describe the ARP request/reply exchange. What makes a Gratuitous ARP different from a
       normal ARP request?

  [3] What are three network problems that manifest as ARP anomalies in a Wireshark capture?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. Why does a host need to know a MAC address to communicate, even though it already
       knows the destination's IP address?

  Q2. ARP requests are broadcasts, but ARP replies are unicasts. Why the asymmetry?

  Q3. In an ARP request, the target MAC address field is set to all zeros
       (00:00:00:00:00:00). Why? What would an ARP reply's target MAC field contain?

  Q4. You capture a trace and see hundreds of ARP requests from the same source IP in less
       than a second. What two very different scenarios could explain this?

  Q5. What are two legitimate reasons a host would send a Gratuitous ARP, and how can you
       tell them apart in a capture?

  Q6. How does an attacker exploit Gratuitous ARP to perform a man-in-the-middle attack?

  Q7. How does a duplicate IP address manifest in a packet capture, and what specific packet
       sequence would you expect to see?

  Q8. A host sends an ARP request for 10.0.2.5 but it's on the 10.0.1.0/24 network. What
       misconfiguration does this reveal?

  Q9. What is the display filter to show only ARP packets where a specific IP address (e.g.,
       192.168.1.100) is the sender?

  Q10.  How would you use a capture filter to record only ARP traffic — and why would you
       want to limit an ARP capture?


────────────────────────────────────────────────────────────
Chapter 17: Analyze Internet Protocol (IPv4/IPv6) Traffic
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] IP sits at Layer 3 — but what exactly does it provide that Layer 2 can't? Think about
       what happens when a packet needs to cross multiple networks to reach its destination.
       What role does IP play in making that possible, and why does it not guarantee
       delivery?

  [2] When you see 'normal' IPv4 traffic in Wireshark, what patterns should you expect?
       Think about how packet sizes vary, what TTL values typically look like, and what the
       Info column tells you. How does knowing the 'normal' baseline help you spot problems?

  [3] What are the most common IPv4-layer problems you'd see in Wireshark? Consider: if a
       host sends a packet with TTL=1 and hits a router, what happens? What about
       fragmentation — when is it necessary and when does it cause performance issues?

  [4] The IPv4 header has 13+ fields. Which ones are most critical for network analysis, and
       why? When you see a packet with DSCP markings, what does that tell you? How does the
       Protocol field help you filter traffic without knowing port numbers?

  [5] IPv6 was designed to fix IPv4's limitations — but it also made deliberate
       architectural choices that differ from IPv4. Why does IPv6 eliminate the checksum?
       Why was fragmentation moved to an extension header? What problems does the massive
       address space actually solve beyond just having more addresses?

  [6] The IPv6 base header is fixed at 40 bytes — compare this to IPv4's variable-length
       header. The Next Header field in IPv6 serves a dual purpose. What is it? How does the
       chain of extension headers work, and why does this design provide more flexibility
       than IPv4 options?

  [7] IPv6 uses 128-bit addresses written in hexadecimal — but there are rules for
       shortening them. More importantly, IPv6 has several address types that serve
       different purposes. What is the difference between a link-local address and a global
       unicast address? When would you filter for one vs the other in Wireshark?

  [8] Networks can't switch from IPv4 to IPv6 overnight. Transition mechanisms like 6to4,
       Teredo, and ISATAP allow IPv6 traffic to traverse IPv4 networks. What is the
       fundamental difference between 6to4 and Teredo? When would one fail but the other
       succeed?

  [9] When sharing a trace file for troubleshooting, you might want to hide real IP
       addresses for privacy. How does Wireshark help with this? Separately — if traffic is
       encrypted, what IP-layer information is still visible even though the payload is
       hidden? What can you still analyze?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. What happens when a router receives a packet with TTL=1? What protocol is used to
       notify the sender, and from what IP does that notification come?

  Q2. What is the purpose of the Protocol field in the IPv4 header, and what values would
       you expect for TCP, UDP, and ICMP?

  Q3. A host sends a 1500-byte IP packet onto a link with an MTU of 576 bytes. What happens?
       How many fragments are created and what are their approximate sizes?

  Q4. If DF (Don't Fragment) is set on a packet that is too large for a link's MTU, what
       happens and what does the sender receive back?

  Q5. A VoIP call is experiencing jitter and delay. You check the packets and find DSCP=0
       (Best Effort) on the RTP audio stream. What does this tell you?

  Q6. What is the difference between DSCP and the older ToS (Type of Service) byte? Are they
       mutually exclusive?

  Q7. IPv6 has a simpler, fixed-size header compared to IPv4. What features from the IPv4
       header were removed, and why?

  Q8. On a dual-stack host, what triggers IPv6 to be preferred over IPv4 when connecting to
       a website that supports both?

  Q9. You want to find all packets in a trace that were fragmented (either fragments or
       original oversized packets). What display filter would you use?

  Q10.  A suspect host is using an abnormally low TTL (e.g., TTL=1) on packets it sends. How
       would you filter for this?


────────────────────────────────────────────────────────────
Chapter 18: Analyze Internet Control Message Protocol (ICMPv4/ICMPv6) Traffic
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] ICMP carries error and informational messages — but it doesn't fix problems itself. If
       a router can't forward a packet because TTL expired, it sends ICMP Type 11. But what
       determines whether the SENDING host does anything with that ICMP message? What would
       happen if all ICMP messages were blocked?

  [2] Wireshark shows ICMP using default coloring rules. During a traceroute, you'll see a
       distinctive pattern of alternating Echo Requests and Time Exceeded messages — the
       "striping" effect. What creates this striping? How does traceroute use TTL to map
       routers along a path?

  [3] An ICMP Redirect (Type 5) message from a router tells a host to use a different
       gateway for a specific destination. Why is this message a security concern if not
       sent by a router? What would happen if a malicious host sent a spoofed Redirect
       message?

  [4] ICMP packets do not contain TCP or UDP headers — what does this mean for port-based
       filtering? An ICMP Type 3 (Destination Unreachable) packet embeds the original IP
       header. What specifically does this embedded header allow you to determine?

  [5] ICMPv6 is far more than just the IPv6 equivalent of ICMPv4 — it takes on functions
       that were separate protocols in IPv4. What IPv4 protocols does ICMPv6 completely
       replace? What does this mean for network operation without ICMPv6?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. ICMP is often described as "the network's error-reporting system." What critical
       difference does that make for how ICMP packets appear in a capture compared to TCP or
       UDP?

  Q2. Many security teams configure firewalls to block all ICMP. What two important network
       functions can this break?

  Q3. You see ICMP type 3, code 1 in a trace following a UDP packet. What exactly does this
       mean, and who sent the ICMP message?

  Q4. What is the difference between ICMP type 3 code 1 (Host Unreachable) and ICMP type 3
       code 3 (Port Unreachable)? At what layer does each originate?

  Q5. A ping to a remote host returns with TTL=56 in the Echo Reply. The OS you're testing
       from uses initial TTL=64. What can you infer about the network path?

  Q6. You send 4 pings to a server and receive 3 replies. The missing reply is packet 3.
       What are two possible explanations?

  Q7. Explain the exact mechanism by which traceroute discovers each hop's IP address. What
       specific sequence of events produces each line of traceroute output?

  Q8. A traceroute shows asterisks (*) for hops 8, 9, and 10, but then hops 11 onwards
       respond normally. What does this tell you?

  Q9. What ARP functions does ICMPv6 replace, and through what ICMPv6 message types?

  Q10.  Why is it especially dangerous to block all ICMPv6 at a firewall, compared to
       blocking all ICMPv4?


────────────────────────────────────────────────────────────
Chapter 19: Analyze User Datagram Protocol (UDP) Traffic
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] UDP and TCP are both transport protocols at Layer 4 — but they make opposite
       tradeoffs. If you had to choose between guaranteed delivery and low overhead, when
       would each choice be appropriate? What kind of applications would fail with TCP but
       work fine with UDP?

  [2] Normal UDP communications use destination port numbers to identify the target
       application. DNS typically uses port 53, DHCP uses 67/68, NTP uses 123. Most
       applications use a temporary (ephemeral) port number for the client side. What
       determines the range of ephemeral port numbers, and how does this affect filtering?

  [3] There are very few problems that occur directly with UDP itself — but port-based
       firewalls can cause UDP to appear broken. When a firewall blocks UDP traffic, it may
       silently discard the packets. How can you tell the difference between a silent
       firewall drop and a service that simply isn't running?

  [4] The UDP header is only 8 bytes — just source port, destination port, length, and
       checksum. Why does UDP include a Length field when the IP header already contains a
       Total Length field? And why is the UDP checksum optional (0x0000) in some
       implementations?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. UDP provides no reliability mechanisms. What three guarantees does TCP provide that
       UDP deliberately omits, and why would an application choose UDP despite this?

  Q2. The UDP checksum field is optional (it can be set to 0x0000 to indicate "not
       computed") in IPv4. Why might an application skip the checksum?

  Q3. DNS uses UDP port 53. Under what conditions does DNS switch to TCP, and what do you
       look for in a capture to confirm this switch happened?

  Q4. DHCP uses UDP on both port 67 and port 68. Why are two different ports needed, and
       what is the DHCP conversation sequence you'd expect to see in a capture?

  Q5. You're analyzing a VoIP call that users report as choppy and breaking up. In the RTP
       UDP stream, what specific packet-level evidence would confirm this is packet loss
       rather than a codec or server problem?

  Q6. How does a UDP port-closed condition manifest in a packet capture? Which protocol
       delivers the error notification?

  Q7. You want to find all DNS traffic in a trace including both UDP and TCP DNS. What
       single display filter covers both?

  Q8. How would you build a filter to find all UDP packets that received an ICMP Port
       Unreachable response — i.e., packets that hit a closed UDP port?


────────────────────────────────────────────────────────────
Chapter 20: Analyze Transmission Control Protocol (TCP) Traffic
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] UDP and TCP are both transport protocols — but TCP adds significant complexity. What
       specific guarantees does TCP provide that justify this overhead? In what scenarios
       would you actually choose UDP over TCP despite its lack of reliability guarantees?

  [2] TCP connections go through a well-defined lifecycle: establishment, data transfer,
       termination. The three-way handshake establishes sequence numbers for both sides. Why
       are Initial Sequence Numbers (ISNs) random rather than always starting at 0? What
       does a TCP Reset mean vs a TCP FIN?

  [3] TCP sequence numbers are at the heart of reliable delivery. Given the equation:
       Sequence Number + Bytes of Data Received = Acknowledgment Number Out — what happens
       to the sequence number during the handshake when no data bytes are sent? How does
       Wireshark's Relative Sequence Numbering feature help analysts?

  [4] TCP has two mechanisms for detecting and recovering from packet loss: Duplicate ACKs
       (receiver-driven) and RTO timeout (sender-driven). Which is faster? Under what
       network conditions would you see primarily Duplicate ACK-triggered retransmissions vs
       RTO-triggered retransmissions? What does that tell you about the network?

  [5] Without SACK, if 1 packet in a sequence of 10 is lost, the sender might retransmit all
       10 (stop-and-wait behavior). With SACK, what exactly does the receiver tell the
       sender, and how does this reduce unnecessary retransmissions? Where is SACK
       capability negotiated?

  [6] The TCP Window field advertises how much buffer space the receiver has available. When
       the window size drops to zero, what happens? Why would a receiver advertise a Window
       of zero even when the network path is perfectly healthy? Think about what's happening
       at the application layer in this case.

  [7] The Nagle algorithm and Delayed ACKs both aim to reduce overhead — but they can
       interact badly. Nagle buffers small sends until an ACK arrives. Delayed ACKs wait up
       to 200ms before sending an ACK. What happens when a telnet session
       (character-at-a-time) runs with Nagle enabled? What about when Nagle interacts with
       Delayed ACKs on a file transfer?

  [8] Wireshark's Expert Information automatically flags many TCP problems. When you see a
       'Previous Segment Lost' flag (not a Retransmission, but the loss flag), what does
       that actually mean? How is it different from 'Retransmission'? Why might you see
       'Spurious Retransmission'?

  [9] The TCP header has a Flags field with 9 bits. The SYN, ACK, FIN, RST, PSH bits are
       commonly discussed — but what is the URG bit? When would you see the PSH bit set? And
       what does it mean for analysis if you see 4 NOP bytes in a row in the TCP Options
       area?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. What is the purpose of the TCP checksum, and what does it cover? If a TCP segment has
       a bad checksum, what typically happens?

  Q2. The TCP header has a Data Offset (header length) field. Why does TCP need this when
       UDP's header is always exactly 8 bytes?

  Q3. What specifically is being "synchronized" in the SYN and SYN-ACK packets — and why
       must both sides do it?

  Q4. The MSS (Maximum Segment Size) is negotiated in the TCP options during the handshake.
       What happens if one side has MSS=1460 and the other has MSS=1000? How does this
       affect the connection?

  Q5. What is a duplicate ACK, and why does TCP use three duplicate ACKs as a signal that a
       segment was lost — rather than waiting for a timeout?

  Q6. Wireshark shows "TCP Previous segment not captured." Is this always a sign of packet
       loss, or could it mean something else?

  Q7. A client is receiving data and its receive window drops from 65535 to 0. What does
       this mean, and what happens to the TCP connection?

  Q8. Why does a large bandwidth-delay product (BDP) require large TCP windows? What happens
       if the window is too small for the BDP?

  Q9. What is the difference between a TCP retransmission and a TCP fast retransmission?
       Under what condition does each occur?

  Q10.  You see a TCP RST in the middle of an established connection. What are two scenarios
       that could cause this?

  Q11.  Explain the difference between a TCP FIN teardown and a TCP RST teardown. What does
       each mean for any in-transit data?

  Q12.  In a TCP FIN teardown, why are four packets needed instead of two? Why can't both
       sides close simultaneously in one round-trip?


────────────────────────────────────────────────────────────
Chapter 21: Graph IO Rates and TCP Trends
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] IO Graphs plot packet/byte rates over time. If you open an IO Graph on a trace with
       only one conversation, you see the overall rate. How would you isolate just
       retransmission traffic on the same graph to visually correlate it with overall
       throughput dips?

  [2] Advanced IO Graphs add CALC functions beyond simple packet counting. SUM(*) with
       tcp.len plots TCP data volume. COUNT(*) with tcp.analysis.retransmission plots
       retransmission occurrences. What is the difference between using SUM(tcp.len) vs
       COUNT(*) when analyzing retransmissions?

  [3] You want to compare IO Graphs from two different trace files — a good download and a
       bad download. They were captured at different times. What problem arises when you
       simply merge the two trace files and create an IO Graph? How does the Capinfos -s
       parameter help?

  [4] The TCP Round Trip Time Graph plots the RTT from a data packet to its corresponding
       ACK. It is UNIDIRECTIONAL. If you open a Round Trip Time Graph and see nothing
       plotted, what is the likely cause? And what does a vertical stripe pattern in the RTT
       graph indicate?

  [5] The TCP Time-Sequence Graph (tcptrace) plots each TCP segment as an I-bar. In an ideal
       data transfer, what pattern should you see? What does it mean when the grey receive
       window line moves TOWARD the I-bars? And what happens when the window size line
       TOUCHES the I-bars?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. You have a 10-minute capture of a slow file transfer. Using IO Graphs, what would you
       look for to determine if the slowness is caused by network congestion vs a slow
       server?

  Q2. The IO Graph shows the same traffic level throughout the capture with no visible
       problems. Does this mean there are no TCP issues?

  Q3. You overlay tcp.analysis.flags on an IO Graph and see red spikes that perfectly align
       with the overall traffic peaks. What does this tell you about the likely root cause?

  Q4. How would you use the Y-axis "AVG" value type with the tcp.analysis.ack_rtt field to
       detect latency trends?

  Q5. The TCP Time-Sequence (Stevens) graph shows a flat horizontal line for several seconds
       in the middle of a transfer. What does this mean?

  Q6. The Throughput graph drops to zero repeatedly at regular intervals. What TCP mechanism
       typically causes this pattern?

  Q7. You see a saw-tooth pattern in the Throughput graph. Where would you look in the
       packet list to find the specific packets causing each throughput drop?

  Q8. The RTT graph shows a sudden spike from 5ms to 800ms and then returns to 5ms. What
       could cause this pattern, and is it a problem if it only happens once?


────────────────────────────────────────────────────────────
Chapter 22: Analyze Dynamic Host Configuration Protocol (DHCP/DHCPv6) Traffic
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] DHCP eliminates the need to manually configure IP addresses, subnet masks, gateways,
       and DNS servers on every host. But DHCP relies on broadcasts for initial discovery —
       what happens when a client boots on a different subnet than the DHCP server? What
       device must be present?

  [2] ADHCP client that boots up OUTSIDE its lease time performs a four-packet DORA process.
       A client that boots up INSIDE its lease time only performs two packets. What are
       those two packets and why can it skip Discover and Offer?

  [3] ADHCP Relay Agent (IP Helper) forwards DHCP broadcasts as unicast packets to a remote
       DHCP server. When you see a DHCP packet that has come through a relay agent, what
       field in the DHCP packet identifies the relay agent's IP? How can you use this to
       identify which subnet the client belongs to?

  [4] ADHCP client receives an Offer from the server, sends a Request, receives an ACK, and
       then sends a Decline. What does this sequence mean? What is the client doing between
       the ACK and the Decline? How could a printer cause this problem?

  [5] DHCP packets are variable length. The Client MAC Address field inside the DHCP packet
       serves a specific purpose different from the Ethernet source MAC. What is it? And
       what is the Magic Cookie field?

  [6] DHCPv6 is defined in RFC 3315. Unlike DHCPv4 which uses broadcasts, DHCPv6 uses
       multicast. What specific multicast address do DHCPv6 clients use to contact servers?
       And what is the port range for DHCPv6 (hint: it's different from DHCPv4)?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. Why does the DHCP Request (step 3 of DORA) still broadcast to 255.255.255.255 even
       though the client already received an Offer?

  Q2. A client sends a DHCP Discover but receives no Offer for 30 seconds, then retransmits.
       What are three possible causes?

  Q3. You capture DHCP traffic and see a packet with giaddr = 10.10.5.1. What does this tell
       you about the network topology?

  Q4. How do you filter Wireshark to show only one specific client's complete DORA exchange?

  Q5. A laptop gets an IP address but can't reach any websites. Looking at the DHCP ACK's
       options in Wireshark, what two options would you check first?

  Q6. A client's DHCP Discover contains option 55 (Parameter Request List) listing options
       3, 6, 15, and 51. What is it asking for?

  Q7. You see a DHCP Decline packet in your capture. What network event triggered the client
       to send it?

  Q8. How would you detect a rogue DHCP server using Wireshark?


────────────────────────────────────────────────────────────
Chapter 23: Analyze Hypertext Transfer Protocol (HTTP) Traffic
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] HTTP is a request/response protocol. The client sends a request, the server sends a
       response. HTTP uses TCP — but what's the trade-off of using a new TCP connection for
       every HTTP object? How did HTTP 1.1 Persistent Connections and HTTP Pipelining
       address this?

  [2] When a client loads a web page, the first object downloaded is the HTML page, which
       references many other objects (CSS, JS, images). A web page that appears to load
       slowly might actually have good network performance to the primary server but be slow
       due to dependencies on third-party servers. How would you prove this in Wireshark?

  [3] HTTP clients use GET to retrieve data and POST to send data to the server. POST is
       commonly used for form submissions and file uploads. What display filter would you
       use to filter only HTTP POST requests? And how would you filter all HTTP server
       errors (5xx range)?

  [4] HTTP is a text-based protocol — requests and responses are human-readable. When you
       use the http display filter, you only see packets with HTTP content. But a web
       browsing session also has TCP handshake packets and TCP ACKs. What filter should you
       use to see the COMPLETE HTTP conversation including the TCP handshake?

  [5] The HTTP Load Distribution statistic reveals website dependencies. If you browse
       www.espn.com and create an HTTP Load Distribution report, what would the different
       rows represent? Why would a web page that appears slow show mostly 2xx response codes
       in the HTTP Packet Counter?

  [6] HTTPS uses TLS to create an encrypted channel over TCP port 443. The TLS handshake
       occurs AFTER the TCP handshake. During the TLS Client Hello, the client sends a list
       of cipher suites it supports. Which side selects the actual cipher suite? What is the
       filter to see only TLS handshake packets?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. You Follow a TCP Stream and see HTTP request headers but the response body looks like
       garbled binary. What is the most likely explanation?

  Q2. HTTP/1.1 requires a "Host" header in every request. What problem was this header
       designed to solve?

  Q3. You see HTTP response code 304 Not Modified. What does this mean from a performance
       perspective?

  Q4. You filter http.response.code >= 400 and see hundreds of 404 errors. What might this
       indicate about a client or automated tool?

  Q5. You see Authorization: Basic dXNlcjpwYXNzd29yZA== in an HTTP request header. What does
       this reveal, and how serious is it?

  Q6. What does a Transfer-Encoding: chunked header mean for how the HTTP response body is
       transmitted?

  Q7. You're analyzing HTTP/2 traffic but the content shows as encrypted. What must you
       configure in Wireshark to decrypt it?

  Q8. Using Statistics → HTTP → Packet Counter, you see 40% of responses are 503. What does
       this tell you about the server?


────────────────────────────────────────────────────────────
Chapter 24: Analyze File Transfer Protocol (FTP) Traffic
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] FTP uses TWO separate TCP connections — a command channel and a data channel. The
       command channel stays open for the entire session on port 21. But the data channel is
       opened on demand for each file transfer and directory listing. Why are two channels
       needed? What would be the problem with sending data on the same connection as
       commands?

  [2] FTP clients type 'get' and 'put' at the command line — but what commands actually
       appear in the packets? And FTP servers respond with numerical codes similar to HTTP.
       What do response codes in the 100-299 range indicate vs codes in the 400-599 range?

  [3] In passive mode, the CLIENT establishes the data connection. The PASV command tells
       the server to listen on a port and report back the IP:port. What problem does passive
       mode solve that active mode has? And what does response 227 contain?

  [4] In active mode FTP, the CLIENT sends a PORT command to tell the server where to
       connect for the data channel. Then the SERVER initiates the data connection. What
       security concern does active mode raise? And what is the PORT command format?

  [5] FTP communication problems start with the TCP handshake. If the server doesn't have
       the FTP daemon running, what happens to the SYN packet? If passive mode is blocked by
       a firewall, what sequence of events would you see in Wireshark? How many connection
       attempts does the client make before giving up?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. A user says they uploaded a sensitive file via FTP. What can you determine about that
       transfer using Wireshark?

  Q2. What is the fundamental difference between SFTP and FTP — are they related protocols?

  Q3. An FTP client can log in and list directories but file transfers always fail. Is this
       more consistent with an active or passive mode problem?

  Q4. In Wireshark, you see a PORT command in the FTP control stream. What information does
       it contain and who initiates the data connection?

  Q5. You see FTP response code 530 repeated 10 times. What is happening and what would you
       check to determine if it's a brute-force attack?

  Q6. An FTP session shows TYPE I command. What does this mean for how the file data is
       transmitted?

  Q7. How would you use Wireshark to recover a file transferred over FTP during a security
       incident?

  Q8. You see FTP response code 425 "Can't open data connection." What FTP concept explains
       this, and what configuration change would fix it?


────────────────────────────────────────────────────────────
Chapter 25: Analyze Email Traffic
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] Email uses different protocols for sending vs retrieving. SMTP sends email from client
       to server AND between mail servers. POP3 and IMAP retrieve email from the server.
       What is the key difference between POP3 and IMAP in terms of where email is stored?

  [2] POP3 uses a simple command/response sequence. After authentication, a client typically
       issues RETR followed by DELE to download and remove each message. What does the STAT
       command return? And what does the LIST command do?

  [3] SMTP sends email from client to server and between mail servers. The SMTP server
       greeting is important — it announces the server's identity. What SMTP commands are
       used to deliver an email? And why might you see SMTP traffic in your capture even
       when you're not sending email?

  [4] IMAP keeps email on the server and synchronizes with clients. Unlike POP3 which uses
       simple numbered commands, IMAP tags each command with a client-generated identifier.
       Why? And what IMAP capability allows clients to watch for new mail without polling
       constantly?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. You see an SMTP exchange with "AUTH LOGIN" followed by two base64-encoded strings.
       What are those strings, and are they encrypted?

  Q2. An email server returns SMTP code 550 in response to a RCPT TO command. What does this
       mean?

  Q3. You Follow a POP3 TCP Stream and see "PASS mysecretpassword" in plaintext. What
       security risk does this represent?

  Q4. A POP3 client sends DELE 1 followed by QUIT. When is message 1 actually deleted from
       the server?

  Q5. What is the fundamental operational difference between POP3 and IMAP from the user's
       perspective?

  Q6. You see IMAP command A003 FETCH 1:100 BODY[HEADER]. What is the client doing, and why
       would it request headers only?

  Q7. During a security investigation, you suspect a user sent confidential data via email.
       What Wireshark feature would you use to recover the actual email content as a file?

  Q8. You analyze SMTP traffic and see a different email address in the "MAIL FROM" SMTP
       command versus the "From:" header inside the DATA section. What does this discrepancy
       indicate?


────────────────────────────────────────────────────────────
Chapter 26: Analyze Wireless LAN (802.11) Traffic
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] WLAN (802.11) adds a Layer 2 wireless medium between devices and the wired network.
       WLAN has three frame types: Management (control the connection), Control (help
       management and data frames), and Data (carry payload). When a wireless client wants
       to connect, what sequence of management frames does it exchange with the access
       point?

  [2] You need to capture on a specific channel to see WLAN traffic. What happens to frames
       on other channels? And what is the difference between captured 802.11 frames and what
       you see when Wireshark is set to 'interpreted' mode vs raw mode?

  [3] An access point broadcasts Beacon frames every 100ms (by default) to announce its
       presence. What information is in a Beacon frame? And when a client roams from one AP
       to another, what process does it use to maintain connectivity?

  [4] WLAN problems show up as retransmissions, association failures, and roaming issues.
       When you see a client repeatedly sending Probe Requests with an SSID but never
       receiving a Probe Response, what does this indicate? And what does excessive
       retransmission at the 802.11 layer indicate?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. You start a Wireshark capture on your Wi-Fi interface but only see your own traffic.
       What change do you need to make to capture all wireless traffic in the area?

  Q2. Why does 802.11 need four address fields in its frame header when Ethernet only needs
       two?

  Q3. An attacker is sending deauthentication frames to disconnect users. Why is this
       possible with standard WPA2, and what standard was designed to fix it?

  Q4. What does a Beacon frame contain and why does it need to be sent every 100ms?

  Q5. You capture a WLAN session and see four EAPOL frames after the Association Response.
       What are these frames and why do they matter for traffic decryption?

  Q6. If you have the Wi-Fi password (PSK) and captured the 4-Way Handshake, what Wireshark
       feature lets you decrypt the WPA2 traffic?

  Q7. You see a high rate of type_subtype 0x0c (Deauthentication) frames from the same
       source MAC targeting multiple different clients. What might this indicate?

  Q8. Wireless → WLAN Statistics shows a client with 40% retry rate. What does this
       indicate?


────────────────────────────────────────────────────────────
Chapter 27: Analyze Voice over IP (VoIP) Traffic
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] VoIP uses SIP for signaling (call setup/teardown) and RTP for the actual audio stream.
       SIP uses TCP or UDP; RTP always uses UDP. Why is RTP always UDP and never TCP? And
       what happens to the conversation quality if even a small percentage of RTP packets
       are lost?

  [2] ASIP INVITE starts a call. The response codes are similar to HTTP. What does a 180
       Ringing mean? What does 200 OK mean in the context of a SIP call? And what is the
       difference between a SIP BYE and a SIP CANCEL?

  [3] RTP packets carry audio data with sequence numbers and timestamps. Unlike TCP sequence
       numbers, RTP sequence numbers increment by 1 per packet regardless of payload size.
       RTP timestamps increment by the number of samples per packet. For G.711 (8kHz, 20ms
       packets), how many samples per packet? And how does Wireshark use sequence numbers
       and timestamps to detect lost packets and jitter?

  [4] Common VoIP problems: one-way audio, choppy audio, dropped calls. One-way audio is
       when one side can hear but the other can't. What causes one-way audio in a VoIP call?
       And if you see RTP packets going in both directions but users report choppy audio,
       what would you check in Wireshark?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. A user reports their VoIP call sounds choppy. Before looking at packets, what are the
       three main quality metrics you need to measure?

  Q2. Why does RTP use UDP instead of TCP for voice data?

  Q3. You see a SIP INVITE followed by a 401 Unauthorized response. What does this mean and
       what should the caller send next?

  Q4. After the SIP 200 OK is received, what information from that message allows RTP to
       begin?

  Q5. An RTP stream shows sequence numbers 100, 101, 103, 104 (102 is missing). What metric
       does this affect and can RTP recover the missing packet?

  Q6. An RTP stream shows jitter values increasing from 2ms to 80ms over the call duration.
       What does this indicate?

  Q7. A user reports a 3-second period of silence mid-call. Using Wireshark VoIP tools, how
       would you locate the cause?

  Q8. You use Telephony → VoIP Calls → Player to play back a call but hear nothing. What are
       two possible reasons?


────────────────────────────────────────────────────────────
Chapter 28: Baseline Your Network
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] Abaseline captures 'normal' network behavior — so you can detect deviations. But what
       makes a baseline useful? If you only capture 1 minute of traffic at 2 AM, is that a
       useful baseline? And how do you use a baseline when you're troubleshooting a reported
       problem?

  [2] What Wireshark statistics should you collect for a useful network baseline? Which five
       statistics give the most complete picture of network health?

  [3] You've collected baselines for 3 months. Now users report the network 'feels slow.'
       How do you use your baselines to prove or disprove this? What deviations from
       baseline would confirm a network problem vs an application problem?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. A network engineer says "we don't baseline — we only capture when something breaks."
       What is wrong with this approach?

  Q2. You baseline your network on Tuesday morning. On Thursday the traffic patterns look
       different. Before concluding the network changed, what should you do?

  Q3. Your baseline shows Host A generating 40% of network traffic. A new capture shows Host
       A at 60%. Is this necessarily a problem?

  Q4. Why is the TCP retransmission rate an important baseline metric even when nothing
       appears to be wrong?

  Q5. Your baseline shows DNS at 2% of traffic. A new capture shows DNS at 22%. What are two
       possible causes — one benign and one malicious?

  Q6. Normal ARP in your baseline is 0.3% of traffic. A new capture shows ARP at 15%. What
       are the two most common causes?

  Q7. What information should you record alongside the .pcapng file when creating a
       baseline?

  Q8. You want to compare this week's capture against last month's baseline. Which Wireshark
       statistics view gives you the easiest comparison of protocol distribution?


────────────────────────────────────────────────────────────
Chapter 29: Find the Top Performance Problems
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] Performance problems can originate at many layers. If users report 'the network is
       slow,' where do you start? What is the structured approach to isolating whether the
       problem is at the network level, the server level, or the application level?

  [2] Network congestion occurs when utilization approaches line rate and queuing delays
       mount. But there's another type of 'slow network' that isn't congestion — it's the
       bandwidth-delay product problem. What is this and when does it matter?

  [3] The three most common TCP performance problems are retransmissions, duplicate ACKs,
       and zero windows. Each has a different root cause. Retransmissions indicate packet
       loss. Duplicate ACKs also indicate packet loss. Zero windows indicate the receiver
       can't keep up. Which of these three is most likely to be caused by an application
       problem (not a network problem)?

  [4] Application response time is the time between a client's request and the server's
       first response. How do you measure this in Wireshark? And if the network RTT is 5ms
       but the application response time is 2000ms, where is the problem?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. A user says "the network is slow." You see SYN→SYN-ACK = 3ms, and HTTP GET→first
       response = 8 seconds. Where is the problem and who should you escalate to?

  Q2. What is the single most important display filter to apply first when analyzing any TCP
       performance problem?

  Q3. How do you distinguish "the server is far away (high RTT)" from "the server is slow
       (high processing time)" using Wireshark?

  Q4. A TCP connection has RTT=150ms and a 65,535-byte receive window with no Window
       Scaling. What is the maximum achievable throughput on this connection?

  Q5. You see both tcp.analysis.retransmission and tcp.analysis.fast_retransmission in a
       capture. Which represents the more severe performance impact and why?

  Q6. How do you determine if packet loss is occurring in one direction only?

  Q7. You capture the same HTTP transaction from both the client and the server. The client
       sees a 2-second response time; the server also sees a 2-second delay between
       receiving the GET and sending the response. What does this tell you?

  Q8. Multiple clients simultaneously experience 5-second HTTP response times to the same
       server, but RTT is only 5ms. What is the most likely cause?


────────────────────────────────────────────────────────────
Chapter 30: Network Forensics
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] Network forensics differs from live network troubleshooting in one critical way. What
       is it, and how does it change how you approach a capture file?

  [2] Before analyzing a forensic capture, what three steps should you always take to
       protect the evidence?

  [3] An attacker on the Internet has compromised a host. What information from a Wireshark
       capture can help identify the attacker, and what are the limitations of each?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. An investigator has only NetFlow logs showing the attacker connected to a database
       server on port 5432. Can they determine what SQL queries were executed? Why or why
       not?

  Q2. Why is it critical to work on a copy of the capture file and never the original?

  Q3. After hashing a forensic capture, a colleague adds a Wireshark packet comment and
       saves. What happened to the SHA-256 hash, and what does this mean for evidence
       integrity?

  Q4. A forensic capture shows all events occurring at 3:00 AM local time, but the incident
       is suspected to have happened at 9:00 AM. What should you investigate?

  Q5. You believe a user downloaded malware via HTTP. What Wireshark feature extracts that
       file from the capture?

  Q6. A Telnet session between two hosts is captured. How would you reconstruct the exact
       commands the attacker typed?

  Q7. A capture shows an attack originating from a Chinese IP address. Can you conclude the
       attacker is in China?

  Q8. Two packets in the same capture appear to come from the same IP but have completely
       different TCP SYN option sets. What might this indicate?


────────────────────────────────────────────────────────────
Chapter 31: Network Scanning and Discovery
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] Nmap's most common scan type is the SYN scan. Why is it preferred over a full TCP
       connect scan from a stealth perspective, and what is its key limitation?

  [2] In a SYN scan against a target with ports 22 (open), 80 (open), and 443 (closed), what
       exactly does the Wireshark packet sequence look like for each port?

  [3] You see a sudden spike in your IO Graph. How do you quickly determine if it's a port
       scan vs. a legitimate traffic surge?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. An attacker uses a FIN scan instead of a SYN scan. What advantage does a FIN scan
       provide?

  Q2. What does an ACK scan reveal that a SYN scan does not?

  Q3. In the Conversations view you see one source IP with 65,535 entries in the TCP tab,
       all lasting under 1 second. What is this, and what specifically is being scanned?

  Q4. A SYN scan against a port generates: SYN → SYN-ACK → RST. What does this tell you
       about that port?

  Q5. A packet arrives with TTL=62, and your capture point is 2 hops from the source. What
       was the original TTL and what OS does this suggest?

  Q6. You see a TCP SYN with Window Size=65535, options MSS+NOP+Window Scale+NOP+NOP+SACK.
       Which OS is the likely sender?

  Q7. You want to detect a UDP port scan without seeing the actual scan packets. What ICMP
       traffic would you look for?

  Q8. An ARP sweep of a /24 subnet completes in 0.5 seconds from one host. What filter
       confirms it?


────────────────────────────────────────────────────────────
Chapter 32: Suspect Traffic Analysis
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] Malware that uses HTTP or HTTPS for C2 tries to blend in with normal web traffic. What
       are four behavioral indicators that distinguish malware HTTP traffic from a human
       browsing the web?

  [2] Modern malware uses HTTPS for C2 to blend with normal web traffic. If you can't
       decrypt the TLS, what observable characteristics still reveal C2 behavior?

  [3] Domain Generation Algorithms (DGAs) are used by malware to make C2 domains hard to
       block. What does DGA traffic look like in a DNS capture, and how do you detect it?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. You graph a specific host's outbound connections in IO Graphs and see a perfectly
       regular spike every exactly 30 seconds. What does this suggest and how would you
       investigate?

  Q2. A host makes 200 DNS queries in one minute with 190 returning NXDOMAIN. What malware
       behavior is this?

  Q3. You see regular HTTP GET requests from a host to a legitimate-looking URL, but the
       User-Agent is "Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1)". Why is this
       suspicious in 2025?

  Q4. What is domain fronting and why does it make C2 traffic difficult to block?

  Q5. You see DNS queries with names like "dGhpcyBpcyBkYXRh.evil-domain.com" repeated many
       times. What is happening and what encoding is likely being used?

  Q6. What fields in ICMP would you examine to detect ICMP tunneling?

  Q7. Host 10.0.0.55 generates 300 NXDOMAIN responses in 5 minutes for random-looking
       domains. What is this behavior?

  Q8. How would you write a filter to find DNS traffic NOT on port 53?


────────────────────────────────────────────────────────────
Chapter 33: Command Line Tools
────────────────────────────────────────────────────────────

THINK FIRST — Pre-Reading Reflection Questions:

  [1] tshark is the command-line version of Wireshark. When would you choose tshark over the
       Wireshark GUI?

  [2] tshark uses both capture filters (-f) and display filters (-Y). What is the difference
       and when should you use each?

  [3] editcap can split, trim, and modify capture files. What are three common forensic or
       operational use cases for editcap?

SOCRATIC QUESTIONS (from Study Guide):

  Q1. You need to capture traffic on a Linux server with no GUI, writing to a file for
       exactly 5 minutes. What tshark command would you run?

  Q2. How do you find the names of network interfaces available for capture on a system
       using tshark?

  Q3. Write a tshark command to extract all DNS query names from a capture file, outputting
       only the domain names.

  Q4. What tshark command gives you TCP conversation statistics from a capture file without
       printing individual packets?

  Q5. You have two simultaneous captures from two different servers. How do you merge them
       into one time-ordered file?

  Q6. A capture was taken with the capture machine's clock wrong by exactly +1 hour (1 hour
       fast). How do you fix the timestamps?

  Q7. As part of forensic evidence handling, you need the SHA-256 hash of a capture file
       without opening Wireshark. What command do you use?

  Q8. For maximum capture performance on a busy 10Gbps interface, which Wireshark CLI tool
       should you use and why?

